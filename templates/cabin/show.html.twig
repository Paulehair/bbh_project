{% extends 'base.html.twig' %}

{% block title %}Cabin{% endblock %}

{% block body %}
    <h1>Cabin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cabin.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ cabin.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ cabin.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cabin_index') }}">back to list</a>

    <a href="{{ path('cabin_edit', {'id': cabin.id}) }}">edit</a>

    {{ include('cabin/_delete_form.html.twig') }}
{% endblock %}